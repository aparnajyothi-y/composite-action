name: Composite Action Example
on:
  workflow_dispatch:
jobs:
  build:
    runs-on: ubuntu-22.04
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      - run: cat requirements.txt
      - name: Set up Node.js
        uses: aparnajyothi-y/composite-action-test@main
      - run: cat requirements.txt
